<?php
/**
 * Magedelight
 * Copyright (C) 2019 Magedelight <info@magedelight.com>
 *
 * @category Magedelight
 * @package Magedelight_Sales
 * @copyright Copyright (c) 2019 Mage Delight (http://www.magedelight.com/)
 * @license http://opensource.org/licenses/gpl-3.0.html GNU General Public License,version 3 (GPL-3.0)
 * @author Magedelight <info@magedelight.com>
 */
?>
<?php /** #var $block Magedelight\Vendor\Block\Orde\Cancelgrid */ ?>
<?php
$gridsession = $block->getGridSession();
$orders_cancel = $gridsession['grid_session']['orders_cancel'];
$_orders = $block->getCollection();
$helper = $this->helper('Magedelight\Vendor\Helper\Data');
$tab = $block->getTab();
?>
<form action="<?php echo $block->getSubmitUrl() ?>" method="get" id="vendor-cancelled-order-search" name="vendor_complete_order_search">
    <div class="row cancel-order-list">
        <div class="row-gutter toolbar-wrapper  mb10">
            <div class="col-xs-4">
                <input type="hidden" name="tab" value="<?php echo $tab; ?>">
                <input type="hidden" name="sfrm" value="cancelled">
                <input id="sort-order" type="hidden" name="sort_order" value="<?php echo $orders_cancel['sort_order'] ?>">
                <input id="sort-direction" type="hidden" name="dir" value="<?php echo $orders_cancel['dir'] ?>">
                <div class="block block-search">
                    <div class="content">
                        <div class="field search-catalog">
                            <div class="control">
                                <input placeholder="<?php echo __('Search...') ?>" type="text" value="<?php
                                if ($orders_cancel['q']) {
                                    echo $orders_cancel['q'];
                                } else {
                                    echo $helper->searchText();
                                }
                                ?>" name="q" id="search-order-input-cancelorder" class="input-text vendor search-order-input">
                            </div>
                            <div class="actions">
                                <div class="vendor primary"><button type="submit" class="vendor action login primary"><span><?php echo __('Search') ?></span></button></div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-xs-8">
                <div class="row">
                    <div class="col-xs-3 pt10">
                        <a rel="new" href="javascript:void(0)" class="action-reset search"><?php echo __('Reset') ?></a>
                    </div>
                     <div class="col-xs-9">
                        <div class="toolbar-filter-wrapper">
                        <div class="data-grid-filters-actions-wrap">
                            <div class="data-grid-filters-action-wrap">
                                <span class="clear-filters mr10"><a rel="new" href="javascript:void(0)" class="action action-reset filter"><?php echo __('Clear') ?></a></span>
                                <button class="action secondary action-default _active" type="button" data-action="grid-filter-expand" title="Filters">
                                    <span><?php echo __('Filters') ?></span>
                                </button>
                            </div>
                        </div>
                        <div class="data-grid-filters-actions-wrap">
                            <div class="data-grid-filters-action-wrap">
                                <a class="action primary export" class="action primary export" href="<?php echo $block->getUrl('*/*/exportPost',
                                ['_current' => true, 'sfrm' => 'cancelled']) ?>"><?php echo __('Export') ?>        </a>
                            </div>
                        </div>
                        </div>

                    </div>
                </div>
            </div>



        </div>
    </div>
    <div class="filter-wrapper" style="display: none;">
        <fieldset class="row fieldset">
            <div class="row-gutter filter-row">
                <div class="col-md-6 col-lg-3 filter-section">
                    <fieldset class="fieldset" id="purchased_date_range">
                        <legend class="legend">
                            <span class=""><?php echo __('Purchased Date') ?></span>
                        </legend>
                        <div class="field">
                            <label class="label" for="purchased_date_from">
                                <span><?php echo __('from') ?></span>
                            </label>
                            <div class="control date-control date-field-container">
                                <input class="input-text"
                                       type="text"
                                       name="pdate_from"
                                       placeholder="MM/DD/YYYY"
                                       id="purchased_date_from"
                                       value="<?php
                                       if ($orders_cancel['pdate_from']) {
                                           echo $orders_cancel['pdate_from'];
                                       } else {
                                           echo $this->getRequest()->getParam('purchaseddate')['from'];
                                       }
                                       ?>">
                            </div>
                        </div>
                        <div class="field">
                            <label class="label" for="purchased_date_to">
                                <span><?php echo __('to') ?></span>
                            </label>
                            <div class="control date-control date-field-container">
                                <input class="input-text" 
                                       type="text"  
                                       name="pdate_to"  
                                       placeholder="MM/DD/YYYY"
                                       id="purchased_date_to" 
                                       value="<?php
                                       if ($orders_cancel['pdate_to']) {
                                           echo $orders_cancel['pdate_to'];
                                       } else {
                                           echo $this->getRequest()->getParam('purchaseddate')['to'];
                                       }
                                       ?>">
                            </div>
                        </div>

                    </fieldset>
                </div>
                <div class="col-md-6 col-lg-3 filter-section">
                    <fieldset class="fieldset" >
                        <legend class="legend">
                            <span class=""><?php echo __('Grand Total') ?></span>
                        </legend>
                        <div class="field">
                            <label class="label" for="grndt_from">
                                <span><?php echo __('from') ?></span>
                            </label>
                            <div class="control">
                                <input class="input-text" type="text" placeholder="<?php echo __('Min'); ?>" name="grndt_from" id="grndt_from" value="<?php
                                if ($orders_cancel['grndt_from']) {
                                    echo $orders_cancel['grndt_from'];
                                }
                                ?>">
                            </div>
                        </div>
                        <div class="field">
                            <label class="label" for="grndt_to">
                                <span><?php echo __('to') ?></span>
                            </label>
                            <div class="control">
                                <input class="input-text" type="text" placeholder="<?php echo __('Max'); ?>" name="grndt_to" id="grndt_from" value="<?php
                                if ($orders_cancel['grndt_to']) {
                                    echo $orders_cancel['grndt_to'];
                                }
                                ?>">
                            </div>
                        </div>
                    </fieldset>
                </div>
                <div class="col-md-6 col-lg-3 filter-section">
                    <fieldset class="fieldset" >
                        <legend class="legend">
                            <span class=""><?php echo __('Cancellation Fee') ?></span>
                        </legend> 
                        <div class="field">
                            <label class="label" for="cf-from">
                                <span><?php echo __('from') ?></span>
                            </label>
                            <div class="control">
                                <input class="input-text" type="text" placeholder="<?php echo __('Min'); ?>" name="cf_from" id="cf-from" value="<?php
                                if ($orders_cancel['cf_from']) {
                                    echo $orders_cancel['cf_from'];
                                } else {
                                           echo $this->getRequest()->getParam('cf')['from'];
                                       }
                                ?>">
                            </div>
                        </div>
                        <div class="field">
                            <label class="label" for="cf-to">
                                <span><?php echo __('to') ?></span>
                            </label>
                            <div class="control">
                                <input class="input-text" type="text" placeholder="<?php echo __('Max'); ?>" name="cf_to" id="cf-to" value="<?php
                                if ($orders_cancel['cf_to']) {
                                    echo $orders_cancel['cf_to'];
                                } else {
                                    echo $this->getRequest()->getParam('cf')['to'];
                                }
                                       ?>">
                            </div>
                        </div>
                    </fieldset>
                </div>
            </div>
            <div class="row-gutter filter-row">
                <div class="col-md-4 filter-section">
                    <fieldset class="fieldset field-full-width">
                        <div class="field">
                            <label class="label"  for="increment_id">
                                <span><?php echo __('ID'); ?></span>
                            </label>
                            <div class="control">
                                <input class="input-text" type="text" name="increment_id" placeholder="ID" id="increment_id" value="<?php
                                if ($orders_cancel['increment_id']) {
                                    echo $orders_cancel['increment_id'];
                                } else {
                                    echo $this->getRequest()->getParam('increment_id');
                                }
                                       ?>">
                            </div>
                        </div>
                    </fieldset>
                </div>
            </div>
        </fieldset>
        <div class="row">
        <div class="row-gutter">
            <div class="filters-footer col-md-12">
                <div class="action-toolbar main-actions">
                    <button class="action primary grid-filter-cancel cancel" type="reset" data-action="grid-filter-cancel" >
                        <span><?php echo __('Cancel') ?></span>
                    </button>
                    <button class="action primary" type="submit" data-action="grid-filter-apply" >
                        <span><?php echo __('Apply') ?></span>
                    </button>
                </div>
            </div>
        </div>
        </div>
    </div>
    <input type="hidden" name="session-clear-ordercancel" id="session-clear-ordercancel" value="0"/>

</form>
<?php if ($_orders && count($_orders)): ?>
    <div class="table-wrapper orders-history">

        <table class="data table table-order-items history" id="cancelled-orders-table">

            <caption class="table-caption"><?php echo __('Orders') ?></caption>
            <thead>
                <tr>
                    <th scope="col" sortorder="rvo_vendor_order_id" class="col data-grid-id-cell id sortable"><?php echo __('ID #') ?></th>
                    <th scope="col" sortorder="rvo_created_at" class="col data-grid-date-cell date sortable"><?php echo __('Purchase Date') ?></th>
                    <th scope="col" sortorder="rvp_cancellation_fee" class="col col-cancellation-fee cancellation-fee sortable"><?php echo __('Cancellation Fee') ?></th>
                    <th scope="col" sortorder="rvo_grand_total" class="col col-grand-total total sortable"><?php echo __('Grand Total') ?></th>
                    <th scope="col" class="col actions"><?php echo __('Action') ?></th>
                </tr>
            </thead>
            <tbody>
                <?php foreach ($_orders as $_order): ?>
                    <tr>
                        <td data-th="<?php echo $block->escapeHtml(__('ID #')) ?>" class="col data-grid-id-cell">
                            <table>
                                <tr>
                                    <td align="center">
                                        <strong>
                                            <?= $_order->getRvoIncrementId() ?>
                                        </strong>
                                        <label style="display: inherit;"><?php 
                                         echo __('Cancelled By %1', $block->getOrderCancelledBy($_order->getCancelledBy()) );
                                         ?></label>
                                    </td>
                                </tr>
                                <tr>
                                    <td>
                                        <?= $block->getOrderSummarizedHtml($_order); ?>
                                    </td>
                                </tr>
                            </table>
                        </td>
                        <td data-th="<?php echo $block->escapeHtml(__('Purchase Date')) ?>" class="col data-grid-date-cell"><?php echo $block->getCurrentLocaleDate($_order->getRvoCreatedAt()) ?></td>
                        <td data-th="<?php echo $block->escapeHtml(__('Cancellation Fee')) ?>" class="col data-grid-price-cell"><?php echo $this->helper('Magento\Framework\Pricing\Helper\Data')->currency($_order->getRvpCancellationFee()) ?></td>
                        <td data-th="<?php echo $block->escapeHtml(__('Grand Total')) ?>" class="col data-grid-price-cell"><?php echo $this->helper('Magento\Framework\Pricing\Helper\Data')->currency($_order->getRvoGrandTotal()) ?></td>
                        <td data-th="<?php echo $block->escapeHtml(__('Actions')) ?>" class="col data-grid-actions-cell">
                            <a href="<?php echo $block->getViewUrl($_order) ?>" class="action view">
                                <span><?php echo __('View') ?></span>
                            </a>                            
                        </td>
                    </tr>
    <?php endforeach; ?>

            </tbody>
        </table>
    </div>
    <?php if ($block->getPagerHtml()): ?>
        <div class="order-products-toolbar toolbar bottom"><?php echo $block->getPagerHtml(); ?></div>
    <?php endif ?>
    <script type="text/javascript">
        require([
            'jquery'
                    , 'mage/calendar', /* Calender widget */
        ], function ($) {

            $(document).ready(function () {

                /*
                 * Sorting feature
                 */
                $currentSortOrder = $('#sort-order').val();
                $currentSortDir = $('#sort-direction').val();

                if ($currentSortOrder != null && $currentSortOrder != '') {
                    $sortDirClass = ($currentSortDir == 'DESC') ? 'descend' : 'ascend';
                    $('#cancelled-orders-table th[sortorder="' + $currentSortOrder + '"]').addClass($sortDirClass);
                }
                $sortDir = 'ASC';

                $('#cancelled-orders-table .sortable').click(function ($e) {
                    $sortOrder = $(this).attr('sortorder');
                    if ($currentSortOrder == $sortOrder) {
                        $sortDir = ($currentSortDir == 'DESC') ? 'ASC' : 'DESC';
                    }
                    $('#sort-order').val($sortOrder); /*for post */
                    $('#sort-direction').val($sortDir); /*for post */
                    $('#vendor-cancelled-order-search').submit();
                });





            });


        });
    </script>       
<?php else : ?>    
    <script type="text/javascript">
        require(['jquery'], function ($) {
            $(document).ready(function () {
                /*  debugger; */
                $value = "false";
                $('input[type=text]').each(function () {
                    var text_value = $(this).val();
                    if (text_value != '')
                    {
                        $value = "true";
                    }

                })
                if (jQuery('#search-order-input-cancelorder')[0].value != "" || $value == "true") {
                    jQuery('#cancelorder-msg span').html('<?php echo __("Your search result could not find.")?>');
                    //jQuery('#empty-msg-neworder')[0].html("Your search result could not find"); 
                } else {
                    jQuery('#cancelorder-msg span').html("<?php echo __("We couldn't find any records.")?>");
                }

            });
        });
    </script>
    <div class="table-wrapper orders-history">
        <table class="data table table-order-items history" id="new-orders-table">
            <thead>
                <tr>
                    <th scope="col" sortorder="rvo_vendor_order_id" class="col id sortable"><?php echo __('ID #') ?></th>
                    <th scope="col" sortorder="rvo_created_at" class="col date sortable"><?php echo __('Purchase Date') ?></th>
                    <th scope="col" sortorder="rvp_cancellation_fee" class="col cancellation-fee sortable"><?php echo __('Cancellation Fee') ?></th>
                    <th scope="col" sortorder="rvo_grand_total" class="col total sortable"><?php echo __('Grand Total') ?></th>
                    <th scope="col" class="col actions"><?php echo __('Action') ?></th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td colspan="7">                        
                        <div id="cancelorder-msg" class="message info empty"><span></span></div>
                    </td>
                </tr>
            </tbody>
        </table>
    </div>    
<?php endif; ?>
<script type="text/javascript">
    require([
        'jquery',
        'mage/calendar'
    ], function ($) {

        $(".action-default").click(function () {
            $(this).parents('.row').next('.filter-wrapper').toggle(500);
        });

        $(".grid-filter-cancel").click(function () {
            $(this).parents('.filter-wrapper').toggle(500);
        });

        $("button.grid-filter-clear-all").click(function ($e) {
            $form = $(this).parents('form');
            $(':input:not([type=hidden])', $form).val("");
            $($form).submit();
        });

        $("#purchased_date_range").dateRange({
            buttonText: "<?php echo __('Select Date') ?>",
            showOn: "button",
            from: {
                id: "purchased_date_from"
            },
            to: {
                id: "purchased_date_to"
            }
        });

        $(".action-reset").click(function () {
            $form = $(this).parents('form');
            if ($(this).hasClass('search')) {
                $parent = $('.block-search', $form);
            } else {
                $parent = $('.filter-wrapper', $form);
            }
            $(':input:not([type=hidden])', $parent).val("");
            jQuery('#session-clear-ordercancel').val(1);
            $($form).submit();
        });

    });
</script>
